/*
  File autogenerated by gengetopt version 2.22.5
  generated with the following command:
  /usr/bin/gengetopt -iSeekIterative.ggo --default-optional -u -N -e 

  The developers of gengetopt consider the fixed text that goes in all
  gengetopt output files to be in the public domain:
  we make no copyright claims on it.
*/

/* If we use autoconf.  */
#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#ifndef FIX_UNUSED
#define FIX_UNUSED(X) (void) (X) /* avoid warnings for unused params */
#endif

#include <getopt.h>

#include "cmdline.h"

const char *gengetopt_args_info_purpose = "Search based on iterative algorithm";

const char *gengetopt_args_info_usage = "Usage: SeekIterative [OPTIONS]... [FILES]...";

const char *gengetopt_args_info_description = "";

const char *gengetopt_args_info_help[] = {
  "      --help                    Print help and exit",
  "      --version                 Print version and exit",
  "\nMode:",
  "  -j, --tdab                    Traditional DAB mode  (default=off)",
  "  -d, --dab                     Sparse Dab mode  (default=off)",
  "  -e, --combined                Combined-dab mode  (default=off)",
  "  -f, --test                    Test mode  (default=off)",
  "  -g, --testcount               Test count mode  (default=off)",
  "  -h, --testcombined            Test count mode  (default=off)",
  "  -v, --visualize               Visualization mode  (default=off)",
  "\nCombined-DAB / Visualization mode:",
  "  -b, --dab_basename=filename   Combined-dab basename, also shared with Test \n                                  Mode",
  "  -t, --top_genes=INT           Top genes to visualize (for combined-dab)  \n                                  (default=`100')",
  "  -E, --generate_dot            Generate a dot file (for combined-dab)  \n                                  (default=off)",
  "  -P, --print_distr             Print distribution of edge values  \n                                  (default=off)",
  "\nVisualization mode:",
  "  -c, --cutoff=FLOAT            Cutoff value  (default=`0.0001')",
  "  -G, --genome=filename         Genome mapping file",
  "\nTraditional DAB mode (see also: -Z, -Y, -S):",
  "  -J, --tdab_list=filename      DAB list",
  "  -a, --gavg_dir=directory      Gene average directory (.gavg files)  \n                                  (default=`NA')",
  "\nSparse DAB mode (see also: -Z, -Y, -S):",
  "  -V, --dab_list=filename       DAB list",
  "  -I, --num_iter=INT            Number of iterations  (default=`0')",
  "  -T, --default_type=INT        Default gene index type (choose unsigned short \n                                  for genes, or unsigned int (32-bit) for \n                                  transcripts) (required for DAB mode) (0 - \n                                  unsigned int, 1 - unsigned short)  \n                                  (default=`-1')",
  "  -R, --rbp_p=FLOAT             RBP p parameter (must be specified) (p<1.0) \n                                  (recommended > 0.95)  (default=`-1')",
  "  -M, --max_rank=INT            Maximum rank number in the sparse DAB matrix \n                                  (must be specified)  (default=`-1')",
  "  -H, --dset_cutoff_file=filename\n                                Dataset score cutoff file  (default=`NA')",
  "  -n, --norm_mode=STRING        Normalization method: rank - rank-normalize \n                                  matrix, subtract_z - subtract-z-normalize \n                                  matrix  (possible values=\"rank\", \n                                  \"subtract_z\", \"NA\" default=`NA')",
  "  -N, --exp=FLOAT               Raise the z-score to the power of this value \n                                  (for --norm_mode=subtract_z)  \n                                  (default=`-1.0')",
  "\nInput:",
  "  -i, --input=filename          Gene mapping file",
  "  -q, --query=filename          Query file",
  "  -F, --dab_dir=directory       DAB directory",
  "  -Q, --not_query=filename      NOT Query file (optional, for combined-DAB)  \n                                  (default=`NA')",
  "  -Z, --threshold_q=FLOAT       Fraction of query genes need to be present in a \n                                  dataset  (default=`0')",
  "  -Y, --threshold_g=FLOAT       Fraction of datasets that must contain a gene \n                                  to put it in ranking (important if individual \n                                  datasets have very different gene coverage, \n                                  and for datasets with small gene-size)  \n                                  (default=`0.50')",
  "  -S, --tsearch_mode=STRING     Search mode: equal weighted (eq) or CV LOI \n                                  (cv_loi) or SPELL (spell) (Applicable if DAB \n                                  list contains more than 1 dataset). (Required \n                                  for --tdab and --dab modes)  (possible \n                                  values=\"eq\", \"cv_loi\", \"spell\", \"NA\" \n                                  default=`NA')",
  "  -x, --debug                   Debug mode  (default=off)",
  "\nOutput:",
  "  -D, --dir_out=directory       Output directory",
    0
};

typedef enum {ARG_NO
  , ARG_FLAG
  , ARG_STRING
  , ARG_INT
  , ARG_FLOAT
} cmdline_parser_arg_type;

static
void clear_given (struct gengetopt_args_info *args_info);
static
void clear_args (struct gengetopt_args_info *args_info);

static int
cmdline_parser_internal (int argc, char **argv, struct gengetopt_args_info *args_info,
                        struct cmdline_parser_params *params, const char *additional_error);

static int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error);

const char *cmdline_parser_norm_mode_values[] = {"rank", "subtract_z", "NA", 0}; /*< Possible values for norm_mode. */
const char *cmdline_parser_tsearch_mode_values[] = {"eq", "cv_loi", "spell", "NA", 0}; /*< Possible values for tsearch_mode. */

static char *
gengetopt_strdup (const char *s);

static
void clear_given (struct gengetopt_args_info *args_info)
{
  args_info->help_given = 0 ;
  args_info->version_given = 0 ;
  args_info->tdab_given = 0 ;
  args_info->dab_given = 0 ;
  args_info->combined_given = 0 ;
  args_info->test_given = 0 ;
  args_info->testcount_given = 0 ;
  args_info->testcombined_given = 0 ;
  args_info->visualize_given = 0 ;
  args_info->dab_basename_given = 0 ;
  args_info->top_genes_given = 0 ;
  args_info->generate_dot_given = 0 ;
  args_info->print_distr_given = 0 ;
  args_info->cutoff_given = 0 ;
  args_info->genome_given = 0 ;
  args_info->tdab_list_given = 0 ;
  args_info->gavg_dir_given = 0 ;
  args_info->dab_list_given = 0 ;
  args_info->num_iter_given = 0 ;
  args_info->default_type_given = 0 ;
  args_info->rbp_p_given = 0 ;
  args_info->max_rank_given = 0 ;
  args_info->dset_cutoff_file_given = 0 ;
  args_info->norm_mode_given = 0 ;
  args_info->exp_given = 0 ;
  args_info->input_given = 0 ;
  args_info->query_given = 0 ;
  args_info->dab_dir_given = 0 ;
  args_info->not_query_given = 0 ;
  args_info->threshold_q_given = 0 ;
  args_info->threshold_g_given = 0 ;
  args_info->tsearch_mode_given = 0 ;
  args_info->debug_given = 0 ;
  args_info->dir_out_given = 0 ;
}

static
void clear_args (struct gengetopt_args_info *args_info)
{
  FIX_UNUSED (args_info);
  args_info->tdab_flag = 0;
  args_info->dab_flag = 0;
  args_info->combined_flag = 0;
  args_info->test_flag = 0;
  args_info->testcount_flag = 0;
  args_info->testcombined_flag = 0;
  args_info->visualize_flag = 0;
  args_info->dab_basename_arg = NULL;
  args_info->dab_basename_orig = NULL;
  args_info->top_genes_arg = 100;
  args_info->top_genes_orig = NULL;
  args_info->generate_dot_flag = 0;
  args_info->print_distr_flag = 0;
  args_info->cutoff_arg = 0.0001;
  args_info->cutoff_orig = NULL;
  args_info->genome_arg = NULL;
  args_info->genome_orig = NULL;
  args_info->tdab_list_arg = NULL;
  args_info->tdab_list_orig = NULL;
  args_info->gavg_dir_arg = gengetopt_strdup ("NA");
  args_info->gavg_dir_orig = NULL;
  args_info->dab_list_arg = NULL;
  args_info->dab_list_orig = NULL;
  args_info->num_iter_arg = 0;
  args_info->num_iter_orig = NULL;
  args_info->default_type_arg = -1;
  args_info->default_type_orig = NULL;
  args_info->rbp_p_arg = -1;
  args_info->rbp_p_orig = NULL;
  args_info->max_rank_arg = -1;
  args_info->max_rank_orig = NULL;
  args_info->dset_cutoff_file_arg = gengetopt_strdup ("NA");
  args_info->dset_cutoff_file_orig = NULL;
  args_info->norm_mode_arg = gengetopt_strdup ("NA");
  args_info->norm_mode_orig = NULL;
  args_info->exp_arg = -1.0;
  args_info->exp_orig = NULL;
  args_info->input_arg = NULL;
  args_info->input_orig = NULL;
  args_info->query_arg = NULL;
  args_info->query_orig = NULL;
  args_info->dab_dir_arg = NULL;
  args_info->dab_dir_orig = NULL;
  args_info->not_query_arg = gengetopt_strdup ("NA");
  args_info->not_query_orig = NULL;
  args_info->threshold_q_arg = 0;
  args_info->threshold_q_orig = NULL;
  args_info->threshold_g_arg = 0.50;
  args_info->threshold_g_orig = NULL;
  args_info->tsearch_mode_arg = gengetopt_strdup ("NA");
  args_info->tsearch_mode_orig = NULL;
  args_info->debug_flag = 0;
  args_info->dir_out_arg = NULL;
  args_info->dir_out_orig = NULL;
  
}

static
void init_args_info(struct gengetopt_args_info *args_info)
{


  args_info->help_help = gengetopt_args_info_help[0] ;
  args_info->version_help = gengetopt_args_info_help[1] ;
  args_info->tdab_help = gengetopt_args_info_help[3] ;
  args_info->dab_help = gengetopt_args_info_help[4] ;
  args_info->combined_help = gengetopt_args_info_help[5] ;
  args_info->test_help = gengetopt_args_info_help[6] ;
  args_info->testcount_help = gengetopt_args_info_help[7] ;
  args_info->testcombined_help = gengetopt_args_info_help[8] ;
  args_info->visualize_help = gengetopt_args_info_help[9] ;
  args_info->dab_basename_help = gengetopt_args_info_help[11] ;
  args_info->top_genes_help = gengetopt_args_info_help[12] ;
  args_info->generate_dot_help = gengetopt_args_info_help[13] ;
  args_info->print_distr_help = gengetopt_args_info_help[14] ;
  args_info->cutoff_help = gengetopt_args_info_help[16] ;
  args_info->genome_help = gengetopt_args_info_help[17] ;
  args_info->tdab_list_help = gengetopt_args_info_help[19] ;
  args_info->gavg_dir_help = gengetopt_args_info_help[20] ;
  args_info->dab_list_help = gengetopt_args_info_help[22] ;
  args_info->num_iter_help = gengetopt_args_info_help[23] ;
  args_info->default_type_help = gengetopt_args_info_help[24] ;
  args_info->rbp_p_help = gengetopt_args_info_help[25] ;
  args_info->max_rank_help = gengetopt_args_info_help[26] ;
  args_info->dset_cutoff_file_help = gengetopt_args_info_help[27] ;
  args_info->norm_mode_help = gengetopt_args_info_help[28] ;
  args_info->exp_help = gengetopt_args_info_help[29] ;
  args_info->input_help = gengetopt_args_info_help[31] ;
  args_info->query_help = gengetopt_args_info_help[32] ;
  args_info->dab_dir_help = gengetopt_args_info_help[33] ;
  args_info->not_query_help = gengetopt_args_info_help[34] ;
  args_info->threshold_q_help = gengetopt_args_info_help[35] ;
  args_info->threshold_g_help = gengetopt_args_info_help[36] ;
  args_info->tsearch_mode_help = gengetopt_args_info_help[37] ;
  args_info->debug_help = gengetopt_args_info_help[38] ;
  args_info->dir_out_help = gengetopt_args_info_help[40] ;
  
}

void
cmdline_parser_print_version (void)
{
  printf ("%s %s\n",
     (strlen(CMDLINE_PARSER_PACKAGE_NAME) ? CMDLINE_PARSER_PACKAGE_NAME : CMDLINE_PARSER_PACKAGE),
     CMDLINE_PARSER_VERSION);
}

static void print_help_common(void) {
  cmdline_parser_print_version ();

  if (strlen(gengetopt_args_info_purpose) > 0)
    printf("\n%s\n", gengetopt_args_info_purpose);

  if (strlen(gengetopt_args_info_usage) > 0)
    printf("\n%s\n", gengetopt_args_info_usage);

  printf("\n");

  if (strlen(gengetopt_args_info_description) > 0)
    printf("%s\n\n", gengetopt_args_info_description);
}

void
cmdline_parser_print_help (void)
{
  int i = 0;
  print_help_common();
  while (gengetopt_args_info_help[i])
    printf("%s\n", gengetopt_args_info_help[i++]);
}

void
cmdline_parser_init (struct gengetopt_args_info *args_info)
{
  clear_given (args_info);
  clear_args (args_info);
  init_args_info (args_info);

  args_info->inputs = 0;
  args_info->inputs_num = 0;
}

void
cmdline_parser_params_init(struct cmdline_parser_params *params)
{
  if (params)
    { 
      params->override = 0;
      params->initialize = 1;
      params->check_required = 1;
      params->check_ambiguity = 0;
      params->print_errors = 1;
    }
}

struct cmdline_parser_params *
cmdline_parser_params_create(void)
{
  struct cmdline_parser_params *params = 
    (struct cmdline_parser_params *)malloc(sizeof(struct cmdline_parser_params));
  cmdline_parser_params_init(params);  
  return params;
}

static void
free_string_field (char **s)
{
  if (*s)
    {
      free (*s);
      *s = 0;
    }
}


static void
cmdline_parser_release (struct gengetopt_args_info *args_info)
{
  unsigned int i;
  free_string_field (&(args_info->dab_basename_arg));
  free_string_field (&(args_info->dab_basename_orig));
  free_string_field (&(args_info->top_genes_orig));
  free_string_field (&(args_info->cutoff_orig));
  free_string_field (&(args_info->genome_arg));
  free_string_field (&(args_info->genome_orig));
  free_string_field (&(args_info->tdab_list_arg));
  free_string_field (&(args_info->tdab_list_orig));
  free_string_field (&(args_info->gavg_dir_arg));
  free_string_field (&(args_info->gavg_dir_orig));
  free_string_field (&(args_info->dab_list_arg));
  free_string_field (&(args_info->dab_list_orig));
  free_string_field (&(args_info->num_iter_orig));
  free_string_field (&(args_info->default_type_orig));
  free_string_field (&(args_info->rbp_p_orig));
  free_string_field (&(args_info->max_rank_orig));
  free_string_field (&(args_info->dset_cutoff_file_arg));
  free_string_field (&(args_info->dset_cutoff_file_orig));
  free_string_field (&(args_info->norm_mode_arg));
  free_string_field (&(args_info->norm_mode_orig));
  free_string_field (&(args_info->exp_orig));
  free_string_field (&(args_info->input_arg));
  free_string_field (&(args_info->input_orig));
  free_string_field (&(args_info->query_arg));
  free_string_field (&(args_info->query_orig));
  free_string_field (&(args_info->dab_dir_arg));
  free_string_field (&(args_info->dab_dir_orig));
  free_string_field (&(args_info->not_query_arg));
  free_string_field (&(args_info->not_query_orig));
  free_string_field (&(args_info->threshold_q_orig));
  free_string_field (&(args_info->threshold_g_orig));
  free_string_field (&(args_info->tsearch_mode_arg));
  free_string_field (&(args_info->tsearch_mode_orig));
  free_string_field (&(args_info->dir_out_arg));
  free_string_field (&(args_info->dir_out_orig));
  
  
  for (i = 0; i < args_info->inputs_num; ++i)
    free (args_info->inputs [i]);

  if (args_info->inputs_num)
    free (args_info->inputs);

  clear_given (args_info);
}

/**
 * @param val the value to check
 * @param values the possible values
 * @return the index of the matched value:
 * -1 if no value matched,
 * -2 if more than one value has matched
 */
static int
check_possible_values(const char *val, const char *values[])
{
  int i, found, last;
  size_t len;

  if (!val)   /* otherwise strlen() crashes below */
    return -1; /* -1 means no argument for the option */

  found = last = 0;

  for (i = 0, len = strlen(val); values[i]; ++i)
    {
      if (strncmp(val, values[i], len) == 0)
        {
          ++found;
          last = i;
          if (strlen(values[i]) == len)
            return i; /* exact macth no need to check more */
        }
    }

  if (found == 1) /* one match: OK */
    return last;

  return (found ? -2 : -1); /* return many values or none matched */
}


static void
write_into_file(FILE *outfile, const char *opt, const char *arg, const char *values[])
{
  int found = -1;
  if (arg) {
    if (values) {
      found = check_possible_values(arg, values);      
    }
    if (found >= 0)
      fprintf(outfile, "%s=\"%s\" # %s\n", opt, arg, values[found]);
    else
      fprintf(outfile, "%s=\"%s\"\n", opt, arg);
  } else {
    fprintf(outfile, "%s\n", opt);
  }
}


int
cmdline_parser_dump(FILE *outfile, struct gengetopt_args_info *args_info)
{
  int i = 0;

  if (!outfile)
    {
      fprintf (stderr, "%s: cannot dump options to stream\n", CMDLINE_PARSER_PACKAGE);
      return EXIT_FAILURE;
    }

  if (args_info->help_given)
    write_into_file(outfile, "help", 0, 0 );
  if (args_info->version_given)
    write_into_file(outfile, "version", 0, 0 );
  if (args_info->tdab_given)
    write_into_file(outfile, "tdab", 0, 0 );
  if (args_info->dab_given)
    write_into_file(outfile, "dab", 0, 0 );
  if (args_info->combined_given)
    write_into_file(outfile, "combined", 0, 0 );
  if (args_info->test_given)
    write_into_file(outfile, "test", 0, 0 );
  if (args_info->testcount_given)
    write_into_file(outfile, "testcount", 0, 0 );
  if (args_info->testcombined_given)
    write_into_file(outfile, "testcombined", 0, 0 );
  if (args_info->visualize_given)
    write_into_file(outfile, "visualize", 0, 0 );
  if (args_info->dab_basename_given)
    write_into_file(outfile, "dab_basename", args_info->dab_basename_orig, 0);
  if (args_info->top_genes_given)
    write_into_file(outfile, "top_genes", args_info->top_genes_orig, 0);
  if (args_info->generate_dot_given)
    write_into_file(outfile, "generate_dot", 0, 0 );
  if (args_info->print_distr_given)
    write_into_file(outfile, "print_distr", 0, 0 );
  if (args_info->cutoff_given)
    write_into_file(outfile, "cutoff", args_info->cutoff_orig, 0);
  if (args_info->genome_given)
    write_into_file(outfile, "genome", args_info->genome_orig, 0);
  if (args_info->tdab_list_given)
    write_into_file(outfile, "tdab_list", args_info->tdab_list_orig, 0);
  if (args_info->gavg_dir_given)
    write_into_file(outfile, "gavg_dir", args_info->gavg_dir_orig, 0);
  if (args_info->dab_list_given)
    write_into_file(outfile, "dab_list", args_info->dab_list_orig, 0);
  if (args_info->num_iter_given)
    write_into_file(outfile, "num_iter", args_info->num_iter_orig, 0);
  if (args_info->default_type_given)
    write_into_file(outfile, "default_type", args_info->default_type_orig, 0);
  if (args_info->rbp_p_given)
    write_into_file(outfile, "rbp_p", args_info->rbp_p_orig, 0);
  if (args_info->max_rank_given)
    write_into_file(outfile, "max_rank", args_info->max_rank_orig, 0);
  if (args_info->dset_cutoff_file_given)
    write_into_file(outfile, "dset_cutoff_file", args_info->dset_cutoff_file_orig, 0);
  if (args_info->norm_mode_given)
    write_into_file(outfile, "norm_mode", args_info->norm_mode_orig, cmdline_parser_norm_mode_values);
  if (args_info->exp_given)
    write_into_file(outfile, "exp", args_info->exp_orig, 0);
  if (args_info->input_given)
    write_into_file(outfile, "input", args_info->input_orig, 0);
  if (args_info->query_given)
    write_into_file(outfile, "query", args_info->query_orig, 0);
  if (args_info->dab_dir_given)
    write_into_file(outfile, "dab_dir", args_info->dab_dir_orig, 0);
  if (args_info->not_query_given)
    write_into_file(outfile, "not_query", args_info->not_query_orig, 0);
  if (args_info->threshold_q_given)
    write_into_file(outfile, "threshold_q", args_info->threshold_q_orig, 0);
  if (args_info->threshold_g_given)
    write_into_file(outfile, "threshold_g", args_info->threshold_g_orig, 0);
  if (args_info->tsearch_mode_given)
    write_into_file(outfile, "tsearch_mode", args_info->tsearch_mode_orig, cmdline_parser_tsearch_mode_values);
  if (args_info->debug_given)
    write_into_file(outfile, "debug", 0, 0 );
  if (args_info->dir_out_given)
    write_into_file(outfile, "dir_out", args_info->dir_out_orig, 0);
  

  i = EXIT_SUCCESS;
  return i;
}

int
cmdline_parser_file_save(const char *filename, struct gengetopt_args_info *args_info)
{
  FILE *outfile;
  int i = 0;

  outfile = fopen(filename, "w");

  if (!outfile)
    {
      fprintf (stderr, "%s: cannot open file for writing: %s\n", CMDLINE_PARSER_PACKAGE, filename);
      return EXIT_FAILURE;
    }

  i = cmdline_parser_dump(outfile, args_info);
  fclose (outfile);

  return i;
}

void
cmdline_parser_free (struct gengetopt_args_info *args_info)
{
  cmdline_parser_release (args_info);
}

/** @brief replacement of strdup, which is not standard */
char *
gengetopt_strdup (const char *s)
{
  char *result = 0;
  if (!s)
    return result;

  result = (char*)malloc(strlen(s) + 1);
  if (result == (char*)0)
    return (char*)0;
  strcpy(result, s);
  return result;
}

int
cmdline_parser (int argc, char **argv, struct gengetopt_args_info *args_info)
{
  return cmdline_parser2 (argc, argv, args_info, 0, 1, 1);
}

int
cmdline_parser_ext (int argc, char **argv, struct gengetopt_args_info *args_info,
                   struct cmdline_parser_params *params)
{
  int result;
  result = cmdline_parser_internal (argc, argv, args_info, params, 0);

  return result;
}

int
cmdline_parser2 (int argc, char **argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required)
{
  int result;
  struct cmdline_parser_params params;
  
  params.override = override;
  params.initialize = initialize;
  params.check_required = check_required;
  params.check_ambiguity = 0;
  params.print_errors = 1;

  result = cmdline_parser_internal (argc, argv, args_info, &params, 0);

  return result;
}

int
cmdline_parser_required (struct gengetopt_args_info *args_info, const char *prog_name)
{
  int result = EXIT_SUCCESS;

  if (cmdline_parser_required2(args_info, prog_name, 0) > 0)
    result = EXIT_FAILURE;

  return result;
}

int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error)
{
  int error = 0;
  FIX_UNUSED (additional_error);

  /* checks for required options */
  if (! args_info->input_given)
    {
      fprintf (stderr, "%s: '--input' ('-i') option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->query_given)
    {
      fprintf (stderr, "%s: '--query' ('-q') option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->dab_dir_given)
    {
      fprintf (stderr, "%s: '--dab_dir' ('-F') option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  
  /* checks for dependences among options */

  return error;
}


static char *package_name = 0;

/**
 * @brief updates an option
 * @param field the generic pointer to the field to update
 * @param orig_field the pointer to the orig field
 * @param field_given the pointer to the number of occurrence of this option
 * @param prev_given the pointer to the number of occurrence already seen
 * @param value the argument for this option (if null no arg was specified)
 * @param possible_values the possible values for this option (if specified)
 * @param default_value the default value (in case the option only accepts fixed values)
 * @param arg_type the type of this option
 * @param check_ambiguity @see cmdline_parser_params.check_ambiguity
 * @param override @see cmdline_parser_params.override
 * @param no_free whether to free a possible previous value
 * @param multiple_option whether this is a multiple option
 * @param long_opt the corresponding long option
 * @param short_opt the corresponding short option (or '-' if none)
 * @param additional_error possible further error specification
 */
static
int update_arg(void *field, char **orig_field,
               unsigned int *field_given, unsigned int *prev_given, 
               char *value, const char *possible_values[],
               const char *default_value,
               cmdline_parser_arg_type arg_type,
               int check_ambiguity, int override,
               int no_free, int multiple_option,
               const char *long_opt, char short_opt,
               const char *additional_error)
{
  char *stop_char = 0;
  const char *val = value;
  int found;
  char **string_field;
  FIX_UNUSED (field);

  stop_char = 0;
  found = 0;

  if (!multiple_option && prev_given && (*prev_given || (check_ambiguity && *field_given)))
    {
      if (short_opt != '-')
        fprintf (stderr, "%s: `--%s' (`-%c') option given more than once%s\n", 
               package_name, long_opt, short_opt,
               (additional_error ? additional_error : ""));
      else
        fprintf (stderr, "%s: `--%s' option given more than once%s\n", 
               package_name, long_opt,
               (additional_error ? additional_error : ""));
      return 1; /* failure */
    }

  if (possible_values && (found = check_possible_values((value ? value : default_value), possible_values)) < 0)
    {
      if (short_opt != '-')
        fprintf (stderr, "%s: %s argument, \"%s\", for option `--%s' (`-%c')%s\n", 
          package_name, (found == -2) ? "ambiguous" : "invalid", value, long_opt, short_opt,
          (additional_error ? additional_error : ""));
      else
        fprintf (stderr, "%s: %s argument, \"%s\", for option `--%s'%s\n", 
          package_name, (found == -2) ? "ambiguous" : "invalid", value, long_opt,
          (additional_error ? additional_error : ""));
      return 1; /* failure */
    }
    
  if (field_given && *field_given && ! override)
    return 0;
  if (prev_given)
    (*prev_given)++;
  if (field_given)
    (*field_given)++;
  if (possible_values)
    val = possible_values[found];

  switch(arg_type) {
  case ARG_FLAG:
    *((int *)field) = !*((int *)field);
    break;
  case ARG_INT:
    if (val) *((int *)field) = strtol (val, &stop_char, 0);
    break;
  case ARG_FLOAT:
    if (val) *((float *)field) = (float)strtod (val, &stop_char);
    break;
  case ARG_STRING:
    if (val) {
      string_field = (char **)field;
      if (!no_free && *string_field)
        free (*string_field); /* free previous string */
      *string_field = gengetopt_strdup (val);
    }
    break;
  default:
    break;
  };

  /* check numeric conversion */
  switch(arg_type) {
  case ARG_INT:
  case ARG_FLOAT:
    if (val && !(stop_char && *stop_char == '\0')) {
      fprintf(stderr, "%s: invalid numeric value: %s\n", package_name, val);
      return 1; /* failure */
    }
    break;
  default:
    ;
  };

  /* store the original value */
  switch(arg_type) {
  case ARG_NO:
  case ARG_FLAG:
    break;
  default:
    if (value && orig_field) {
      if (no_free) {
        *orig_field = value;
      } else {
        if (*orig_field)
          free (*orig_field); /* free previous string */
        *orig_field = gengetopt_strdup (value);
      }
    }
  };

  return 0; /* OK */
}


int
cmdline_parser_internal (
  int argc, char **argv, struct gengetopt_args_info *args_info,
                        struct cmdline_parser_params *params, const char *additional_error)
{
  int c;	/* Character of the parsed option.  */

  int error = 0;
  struct gengetopt_args_info local_args_info;
  
  int override;
  int initialize;
  int check_required;
  int check_ambiguity;
  
  package_name = argv[0];
  
  override = params->override;
  initialize = params->initialize;
  check_required = params->check_required;
  check_ambiguity = params->check_ambiguity;

  if (initialize)
    cmdline_parser_init (args_info);

  cmdline_parser_init (&local_args_info);

  optarg = 0;
  optind = 0;
  opterr = params->print_errors;
  optopt = '?';

  while (1)
    {
      int option_index = 0;

      static struct option long_options[] = {
        { "help",	0, NULL, 0 },
        { "version",	0, NULL, 0 },
        { "tdab",	0, NULL, 'j' },
        { "dab",	0, NULL, 'd' },
        { "combined",	0, NULL, 'e' },
        { "test",	0, NULL, 'f' },
        { "testcount",	0, NULL, 'g' },
        { "testcombined",	0, NULL, 'h' },
        { "visualize",	0, NULL, 'v' },
        { "dab_basename",	1, NULL, 'b' },
        { "top_genes",	1, NULL, 't' },
        { "generate_dot",	0, NULL, 'E' },
        { "print_distr",	0, NULL, 'P' },
        { "cutoff",	1, NULL, 'c' },
        { "genome",	1, NULL, 'G' },
        { "tdab_list",	1, NULL, 'J' },
        { "gavg_dir",	1, NULL, 'a' },
        { "dab_list",	1, NULL, 'V' },
        { "num_iter",	1, NULL, 'I' },
        { "default_type",	1, NULL, 'T' },
        { "rbp_p",	1, NULL, 'R' },
        { "max_rank",	1, NULL, 'M' },
        { "dset_cutoff_file",	1, NULL, 'H' },
        { "norm_mode",	1, NULL, 'n' },
        { "exp",	1, NULL, 'N' },
        { "input",	1, NULL, 'i' },
        { "query",	1, NULL, 'q' },
        { "dab_dir",	1, NULL, 'F' },
        { "not_query",	1, NULL, 'Q' },
        { "threshold_q",	1, NULL, 'Z' },
        { "threshold_g",	1, NULL, 'Y' },
        { "tsearch_mode",	1, NULL, 'S' },
        { "debug",	0, NULL, 'x' },
        { "dir_out",	1, NULL, 'D' },
        { 0,  0, 0, 0 }
      };

      c = getopt_long (argc, argv, "jdefghvb:t:EPc:G:J:a:V:I:T:R:M:H:n:N:i:q:F:Q:Z:Y:S:xD:", long_options, &option_index);

      if (c == -1) break;	/* Exit from `while (1)' loop.  */

      switch (c)
        {
        case 'j':	/* Traditional DAB mode.  */
        
        
          if (update_arg((void *)&(args_info->tdab_flag), 0, &(args_info->tdab_given),
              &(local_args_info.tdab_given), optarg, 0, 0, ARG_FLAG,
              check_ambiguity, override, 1, 0, "tdab", 'j',
              additional_error))
            goto failure;
        
          break;
        case 'd':	/* Sparse Dab mode.  */
        
        
          if (update_arg((void *)&(args_info->dab_flag), 0, &(args_info->dab_given),
              &(local_args_info.dab_given), optarg, 0, 0, ARG_FLAG,
              check_ambiguity, override, 1, 0, "dab", 'd',
              additional_error))
            goto failure;
        
          break;
        case 'e':	/* Combined-dab mode.  */
        
        
          if (update_arg((void *)&(args_info->combined_flag), 0, &(args_info->combined_given),
              &(local_args_info.combined_given), optarg, 0, 0, ARG_FLAG,
              check_ambiguity, override, 1, 0, "combined", 'e',
              additional_error))
            goto failure;
        
          break;
        case 'f':	/* Test mode.  */
        
        
          if (update_arg((void *)&(args_info->test_flag), 0, &(args_info->test_given),
              &(local_args_info.test_given), optarg, 0, 0, ARG_FLAG,
              check_ambiguity, override, 1, 0, "test", 'f',
              additional_error))
            goto failure;
        
          break;
        case 'g':	/* Test count mode.  */
        
        
          if (update_arg((void *)&(args_info->testcount_flag), 0, &(args_info->testcount_given),
              &(local_args_info.testcount_given), optarg, 0, 0, ARG_FLAG,
              check_ambiguity, override, 1, 0, "testcount", 'g',
              additional_error))
            goto failure;
        
          break;
        case 'h':	/* Test count mode.  */
        
        
          if (update_arg((void *)&(args_info->testcombined_flag), 0, &(args_info->testcombined_given),
              &(local_args_info.testcombined_given), optarg, 0, 0, ARG_FLAG,
              check_ambiguity, override, 1, 0, "testcombined", 'h',
              additional_error))
            goto failure;
        
          break;
        case 'v':	/* Visualization mode.  */
        
        
          if (update_arg((void *)&(args_info->visualize_flag), 0, &(args_info->visualize_given),
              &(local_args_info.visualize_given), optarg, 0, 0, ARG_FLAG,
              check_ambiguity, override, 1, 0, "visualize", 'v',
              additional_error))
            goto failure;
        
          break;
        case 'b':	/* Combined-dab basename, also shared with Test Mode.  */
        
        
          if (update_arg( (void *)&(args_info->dab_basename_arg), 
               &(args_info->dab_basename_orig), &(args_info->dab_basename_given),
              &(local_args_info.dab_basename_given), optarg, 0, 0, ARG_STRING,
              check_ambiguity, override, 0, 0,
              "dab_basename", 'b',
              additional_error))
            goto failure;
        
          break;
        case 't':	/* Top genes to visualize (for combined-dab).  */
        
        
          if (update_arg( (void *)&(args_info->top_genes_arg), 
               &(args_info->top_genes_orig), &(args_info->top_genes_given),
              &(local_args_info.top_genes_given), optarg, 0, "100", ARG_INT,
              check_ambiguity, override, 0, 0,
              "top_genes", 't',
              additional_error))
            goto failure;
        
          break;
        case 'E':	/* Generate a dot file (for combined-dab).  */
        
        
          if (update_arg((void *)&(args_info->generate_dot_flag), 0, &(args_info->generate_dot_given),
              &(local_args_info.generate_dot_given), optarg, 0, 0, ARG_FLAG,
              check_ambiguity, override, 1, 0, "generate_dot", 'E',
              additional_error))
            goto failure;
        
          break;
        case 'P':	/* Print distribution of edge values.  */
        
        
          if (update_arg((void *)&(args_info->print_distr_flag), 0, &(args_info->print_distr_given),
              &(local_args_info.print_distr_given), optarg, 0, 0, ARG_FLAG,
              check_ambiguity, override, 1, 0, "print_distr", 'P',
              additional_error))
            goto failure;
        
          break;
        case 'c':	/* Cutoff value.  */
        
        
          if (update_arg( (void *)&(args_info->cutoff_arg), 
               &(args_info->cutoff_orig), &(args_info->cutoff_given),
              &(local_args_info.cutoff_given), optarg, 0, "0.0001", ARG_FLOAT,
              check_ambiguity, override, 0, 0,
              "cutoff", 'c',
              additional_error))
            goto failure;
        
          break;
        case 'G':	/* Genome mapping file.  */
        
        
          if (update_arg( (void *)&(args_info->genome_arg), 
               &(args_info->genome_orig), &(args_info->genome_given),
              &(local_args_info.genome_given), optarg, 0, 0, ARG_STRING,
              check_ambiguity, override, 0, 0,
              "genome", 'G',
              additional_error))
            goto failure;
        
          break;
        case 'J':	/* DAB list.  */
        
        
          if (update_arg( (void *)&(args_info->tdab_list_arg), 
               &(args_info->tdab_list_orig), &(args_info->tdab_list_given),
              &(local_args_info.tdab_list_given), optarg, 0, 0, ARG_STRING,
              check_ambiguity, override, 0, 0,
              "tdab_list", 'J',
              additional_error))
            goto failure;
        
          break;
        case 'a':	/* Gene average directory (.gavg files).  */
        
        
          if (update_arg( (void *)&(args_info->gavg_dir_arg), 
               &(args_info->gavg_dir_orig), &(args_info->gavg_dir_given),
              &(local_args_info.gavg_dir_given), optarg, 0, "NA", ARG_STRING,
              check_ambiguity, override, 0, 0,
              "gavg_dir", 'a',
              additional_error))
            goto failure;
        
          break;
        case 'V':	/* DAB list.  */
        
        
          if (update_arg( (void *)&(args_info->dab_list_arg), 
               &(args_info->dab_list_orig), &(args_info->dab_list_given),
              &(local_args_info.dab_list_given), optarg, 0, 0, ARG_STRING,
              check_ambiguity, override, 0, 0,
              "dab_list", 'V',
              additional_error))
            goto failure;
        
          break;
        case 'I':	/* Number of iterations.  */
        
        
          if (update_arg( (void *)&(args_info->num_iter_arg), 
               &(args_info->num_iter_orig), &(args_info->num_iter_given),
              &(local_args_info.num_iter_given), optarg, 0, "0", ARG_INT,
              check_ambiguity, override, 0, 0,
              "num_iter", 'I',
              additional_error))
            goto failure;
        
          break;
        case 'T':	/* Default gene index type (choose unsigned short for genes, or unsigned int (32-bit) for transcripts) (required for DAB mode) (0 - unsigned int, 1 - unsigned short).  */
        
        
          if (update_arg( (void *)&(args_info->default_type_arg), 
               &(args_info->default_type_orig), &(args_info->default_type_given),
              &(local_args_info.default_type_given), optarg, 0, "-1", ARG_INT,
              check_ambiguity, override, 0, 0,
              "default_type", 'T',
              additional_error))
            goto failure;
        
          break;
        case 'R':	/* RBP p parameter (must be specified) (p<1.0) (recommended > 0.95).  */
        
        
          if (update_arg( (void *)&(args_info->rbp_p_arg), 
               &(args_info->rbp_p_orig), &(args_info->rbp_p_given),
              &(local_args_info.rbp_p_given), optarg, 0, "-1", ARG_FLOAT,
              check_ambiguity, override, 0, 0,
              "rbp_p", 'R',
              additional_error))
            goto failure;
        
          break;
        case 'M':	/* Maximum rank number in the sparse DAB matrix (must be specified).  */
        
        
          if (update_arg( (void *)&(args_info->max_rank_arg), 
               &(args_info->max_rank_orig), &(args_info->max_rank_given),
              &(local_args_info.max_rank_given), optarg, 0, "-1", ARG_INT,
              check_ambiguity, override, 0, 0,
              "max_rank", 'M',
              additional_error))
            goto failure;
        
          break;
        case 'H':	/* Dataset score cutoff file.  */
        
        
          if (update_arg( (void *)&(args_info->dset_cutoff_file_arg), 
               &(args_info->dset_cutoff_file_orig), &(args_info->dset_cutoff_file_given),
              &(local_args_info.dset_cutoff_file_given), optarg, 0, "NA", ARG_STRING,
              check_ambiguity, override, 0, 0,
              "dset_cutoff_file", 'H',
              additional_error))
            goto failure;
        
          break;
        case 'n':	/* Normalization method: rank - rank-normalize matrix, subtract_z - subtract-z-normalize matrix.  */
        
        
          if (update_arg( (void *)&(args_info->norm_mode_arg), 
               &(args_info->norm_mode_orig), &(args_info->norm_mode_given),
              &(local_args_info.norm_mode_given), optarg, cmdline_parser_norm_mode_values, "NA", ARG_STRING,
              check_ambiguity, override, 0, 0,
              "norm_mode", 'n',
              additional_error))
            goto failure;
        
          break;
        case 'N':	/* Raise the z-score to the power of this value (for --norm_mode=subtract_z).  */
        
        
          if (update_arg( (void *)&(args_info->exp_arg), 
               &(args_info->exp_orig), &(args_info->exp_given),
              &(local_args_info.exp_given), optarg, 0, "-1.0", ARG_FLOAT,
              check_ambiguity, override, 0, 0,
              "exp", 'N',
              additional_error))
            goto failure;
        
          break;
        case 'i':	/* Gene mapping file.  */
        
        
          if (update_arg( (void *)&(args_info->input_arg), 
               &(args_info->input_orig), &(args_info->input_given),
              &(local_args_info.input_given), optarg, 0, 0, ARG_STRING,
              check_ambiguity, override, 0, 0,
              "input", 'i',
              additional_error))
            goto failure;
        
          break;
        case 'q':	/* Query file.  */
        
        
          if (update_arg( (void *)&(args_info->query_arg), 
               &(args_info->query_orig), &(args_info->query_given),
              &(local_args_info.query_given), optarg, 0, 0, ARG_STRING,
              check_ambiguity, override, 0, 0,
              "query", 'q',
              additional_error))
            goto failure;
        
          break;
        case 'F':	/* DAB directory.  */
        
        
          if (update_arg( (void *)&(args_info->dab_dir_arg), 
               &(args_info->dab_dir_orig), &(args_info->dab_dir_given),
              &(local_args_info.dab_dir_given), optarg, 0, 0, ARG_STRING,
              check_ambiguity, override, 0, 0,
              "dab_dir", 'F',
              additional_error))
            goto failure;
        
          break;
        case 'Q':	/* NOT Query file (optional, for combined-DAB).  */
        
        
          if (update_arg( (void *)&(args_info->not_query_arg), 
               &(args_info->not_query_orig), &(args_info->not_query_given),
              &(local_args_info.not_query_given), optarg, 0, "NA", ARG_STRING,
              check_ambiguity, override, 0, 0,
              "not_query", 'Q',
              additional_error))
            goto failure;
        
          break;
        case 'Z':	/* Fraction of query genes need to be present in a dataset.  */
        
        
          if (update_arg( (void *)&(args_info->threshold_q_arg), 
               &(args_info->threshold_q_orig), &(args_info->threshold_q_given),
              &(local_args_info.threshold_q_given), optarg, 0, "0", ARG_FLOAT,
              check_ambiguity, override, 0, 0,
              "threshold_q", 'Z',
              additional_error))
            goto failure;
        
          break;
        case 'Y':	/* Fraction of datasets that must contain a gene to put it in ranking (important if individual datasets have very different gene coverage, and for datasets with small gene-size).  */
        
        
          if (update_arg( (void *)&(args_info->threshold_g_arg), 
               &(args_info->threshold_g_orig), &(args_info->threshold_g_given),
              &(local_args_info.threshold_g_given), optarg, 0, "0.50", ARG_FLOAT,
              check_ambiguity, override, 0, 0,
              "threshold_g", 'Y',
              additional_error))
            goto failure;
        
          break;
        case 'S':	/* Search mode: equal weighted (eq) or CV LOI (cv_loi) or SPELL (spell) (Applicable if DAB list contains more than 1 dataset). (Required for --tdab and --dab modes).  */
        
        
          if (update_arg( (void *)&(args_info->tsearch_mode_arg), 
               &(args_info->tsearch_mode_orig), &(args_info->tsearch_mode_given),
              &(local_args_info.tsearch_mode_given), optarg, cmdline_parser_tsearch_mode_values, "NA", ARG_STRING,
              check_ambiguity, override, 0, 0,
              "tsearch_mode", 'S',
              additional_error))
            goto failure;
        
          break;
        case 'x':	/* Debug mode.  */
        
        
          if (update_arg((void *)&(args_info->debug_flag), 0, &(args_info->debug_given),
              &(local_args_info.debug_given), optarg, 0, 0, ARG_FLAG,
              check_ambiguity, override, 1, 0, "debug", 'x',
              additional_error))
            goto failure;
        
          break;
        case 'D':	/* Output directory.  */
        
        
          if (update_arg( (void *)&(args_info->dir_out_arg), 
               &(args_info->dir_out_orig), &(args_info->dir_out_given),
              &(local_args_info.dir_out_given), optarg, 0, 0, ARG_STRING,
              check_ambiguity, override, 0, 0,
              "dir_out", 'D',
              additional_error))
            goto failure;
        
          break;

        case 0:	/* Long option with no short option */
          if (strcmp (long_options[option_index].name, "help") == 0) {
            cmdline_parser_print_help ();
            cmdline_parser_free (&local_args_info);
            exit (EXIT_SUCCESS);
          }

          /* Print version and exit.  */
          if (strcmp (long_options[option_index].name, "version") == 0)
          {
          
          
            if (update_arg( 0 , 
                 0 , &(args_info->version_given),
                &(local_args_info.version_given), optarg, 0, 0, ARG_NO,
                check_ambiguity, override, 0, 0,
                "version", 'V',
                additional_error))
              goto failure;
            cmdline_parser_free (&local_args_info);
            return 0;
          
          }
          
          break;
        case '?':	/* Invalid option.  */
          /* `getopt_long' already printed an error message.  */
          goto failure;

        default:	/* bug: option not considered.  */
          fprintf (stderr, "%s: option unknown: %c%s\n", CMDLINE_PARSER_PACKAGE, c, (additional_error ? additional_error : ""));
          abort ();
        } /* switch */
    } /* while */



  if (check_required)
    {
      error += cmdline_parser_required2 (args_info, argv[0], additional_error);
    }

  cmdline_parser_release (&local_args_info);

  if ( error )
    return (EXIT_FAILURE);

  if (optind < argc)
    {
      int i = 0 ;
      int found_prog_name = 0;
      /* whether program name, i.e., argv[0], is in the remaining args
         (this may happen with some implementations of getopt,
          but surely not with the one included by gengetopt) */

      i = optind;
      while (i < argc)
        if (argv[i++] == argv[0]) {
          found_prog_name = 1;
          break;
        }
      i = 0;

      args_info->inputs_num = argc - optind - found_prog_name;
      args_info->inputs =
        (char **)(malloc ((args_info->inputs_num)*sizeof(char *))) ;
      while (optind < argc)
        if (argv[optind++] != argv[0])
          args_info->inputs[ i++ ] = gengetopt_strdup (argv[optind-1]) ;
    }

  return 0;

failure:
  
  cmdline_parser_release (&local_args_info);
  return (EXIT_FAILURE);
}
